# Base compose — used as-is for both the devcontainer's dev image and the
# production deployment (this is the file Dokploy points to).
# Never publish host ports here — use `expose:` instead of `ports:`.
services:
  app:
    build:
      context: ./webapp
    expose:
      - "3003"
    environment:
      NODE_ENV: production
