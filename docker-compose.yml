# Base compose — production deployment (this is the file Dokploy points to).
# Dev-only additions live in docker-compose.override.yml.
# Never publish host ports here — use `expose:` instead of `ports:`.
services:
  app:
    build:
      context: ./webapp
    expose:
      - "3003"
    environment:
      NODE_ENV: production
