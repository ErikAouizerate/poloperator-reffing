# Dev-only bind-mount targets for named compose volumes (see deploy policy)
volumes/

node_modules/
dist/

# superpowers working artifacts — generated locally, never committed
.superpowers/
docs/superpowers/plans/